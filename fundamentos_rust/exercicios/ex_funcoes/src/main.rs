// aplicaçao com parametros, retorno, logica condicional e reaproveitamento de codigo

// Retorna o maior entre dois valores
fn maior(a: i32, b: i32) -> {
    if a > b {a} else {b}
}

// Retorna a maior entre tres valores, reaproveitando a funçao "maior"
fn 